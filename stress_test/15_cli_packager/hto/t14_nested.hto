# t14_nested.hto - nested function (closure)
fn make_adder(n) {
    fn adder(x) {
        return x + n;
    }
    return adder;
}

let add5 = make_adder(5);
let add10 = make_adder(10);
print(add5(3));
print(add10(3));
