# H# Benchmark 5: Recursive Fibonacci
# Compute fib(30) with naive recursion

fn fib(n) {
    if (n < 2) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

let r = fib(30);
print("fib(30)=" + str(r));
