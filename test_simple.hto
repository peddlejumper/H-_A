# test_simple.hto
let _ai_cfg = {"a": 1};

fn test(cfg) {
    if (cfg == nullptr) {
        return;
    }
    if ("a" in cfg) {
        let x = cfg["a"];
        print(str(x));
    }
}

test({"a": 42});
